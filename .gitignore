# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
/.codex/config.toml

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# lockfiles concurrents : ce projet utilise npm (package-lock.json).
# Un second lockfile diverge en silence — celui de yarn était resté à
# 161 entrées contre 616 côté npm.
yarn.lock
pnpm-lock.yaml
bun.lockb

# outillage local et notes de travail, hors versionning
.claude/
docs/backlog.md
